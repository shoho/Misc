{
  "name": "city-merge",
  "version": "1.0.0",
  "private": true,
  "description": "Merge two cities into a Google concept store scene via OpenAI gpt-image-2, hosted on Cloudflare Pages",
  "scripts": {
    "dev": "wrangler pages dev . --port 8788",
    "deploy": "wrangler pages deploy . --project-name city-merge",
    "tail": "wrangler pages deployment tail --project-name city-merge"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
